<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codeplay.aintrealname">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controller.activities.SignInActivity"
            android:screenOrientation="portrait"
            android:theme="@style/LoginSignupTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".controller.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.activities.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".controller.activities.PrescriptionDetailActivity" />
        <activity android:name=".controller.activities.AddPrognosisActivity" />
        <activity android:name=".controller.activities.MedicationActivity"></activity>
    </application>

</manifest>